# When does "structure protects computation" survive scrutiny? A ledger across twenty tests

**gbranaa4-hue** (independent researcher)
Companion to `PREPRINT.md`. Draws on results from `topological-phononics/`, the Spikeling neuromorphic DSL bridge, the acoustic-vortex quasicrystal reservoir, the acoustic levitator control project, and one real-world-motivated network in `horde-beta-version-1` (jet-engine spike pipeline, 2026-08-09).

---

## Scope note — what this is NOT claiming

An earlier attempt at this kind of synthesis was made and retracted (`cross-substrate-synthesis-finding`, 2026-07-10): the pitch was that one "symmetry-conditional protection" law governs phononic, ternary, and acoustic substrates alike. It was measured, not assumed, and it lost — the acoustic mechanism (symmetry gating a nonlinearity directly) turned out to be a genuinely different phenomenon from the phononic/ternary one (symmetry gating a topological invariant one step removed from computation), and didn't even reproduce across its own substrate class (FEM plate → resonator bank, null).

This document does not repeat that claim. It is not a proposal that one mechanism unifies these results — the substrates below span tight-binding lattices, Duffing-nonlinear mechanical oscillators, magnetic exchange coupling, quasi-periodic tilings, and a staged spiking-neuron pipeline with no lattice structure at all; there is no single physical law connecting them. What is being reported is narrower and, we think, more useful: a **track record**. Across twenty independently pre-registered or independently-designed tests of the same hypothesis *shape* — "an exotic or reinforcing structural feature will make a reservoir/network more computationally robust or capable than the naive baseline" — how often did that hypothesis survive confound-controlled measurement? This is a base-rate finding for calibrating priors on the *next* instance of the hypothesis, not a claim about *why*.

## Method

Each row below is a distinct, real test run in this body of work, with its own pre-registration or explicit hypothesis, its own confound-hunting pass, and its own honest verdict already on record in the source repo or vault entry cited. Nothing here is re-derived from memory of the result — each row was read from the actual source file before being tabulated. Verdicts use four categories: **CONFIRMED** (survived a pre-registered or confound-controlled test), **INCONCLUSIVE** (leans a direction but the confidence interval spans the null), **NULL/REFUTED/REVERSED** (the hypothesis failed, or the effect ran opposite the prediction), **RETRACTED** (an earlier positive read was overturned by a sharper follow-up test on the same claim).

## Results ledger

| # | Substrate | Hypothesis tested | Verdict | Detail | Source |
|---|---|---|---|---|---|
| 1 | SSH resonator chain (idealized tight-binding) | topological phase is more defect-tolerant, linear task | **CONFIRMED** | ~5-8x lower penalty, N≥8, chiral-symmetry conditional | `PREPRINT.md` §3.2-3.3 |
| 2 | SSH resonator chain | defect-tolerance transfers to nonlinear (NARMA10) | **CONFIRMED, weaker** | 67% win-rate, ~2.3x (vs 94%/5x linear) | `PREPRINT.md` §3.5 |
| 3 | SSH resonator chain vs random ESN | topology beats generic redundant connectivity | **REFUTED** | random ESN median penalty 2.7 vs SSH 9.5 | `PREPRINT.md` §3.6 |
| 4 | SSH resonator chain, degree-matched | penalty falls with redundancy; SSH~random at matched sparsity | **REFUTED, wrong direction** | penalty *rises* with connectivity; SSH (a path) uniquely fragile — every interior node is a cut vertex | `PREPRINT.md` §3.6, `narma_redundancy.py` |
| 5 | SSH Duffing mechanical chain, naive coupling | defect-tolerance survives real device physics | **REVERSED** | naive spring coupling breaks chiral symmetry; topological 9.50 vs trivial 3.83 (0.4x, disadvantage) | `PREPRINT.md` §3.7 |
| 6 | SSH Duffing mechanical chain, chirally compensated | restoring chiral symmetry restores the advantage | **INCONCLUSIVE** | win-rate 58%, 95% CI 47-69% (pre-registered bar: CI lower bound > 50%) — does not clear it | `PREPRINT.md` §3.7, `firm_chiral.py` |
| 7 | Acoustic levitator, SSH control-reservoir (real Gor'kov physics, multi-channel sensor task) | topology helps a real onboard control-inference task | **REVERSED** | 1.6x disadvantage, 0/6 seeds won | `ssh_control_reservoir_test.py` |
| 8 | Acoustic levitator, quasicrystal element placement | aperiodic placement suppresses grating lobes (established antenna-array technique) | **NULL** | 1.01x, essentially identical to regular grid | `quasicrystal_array_check.py` |
| 9 | Acoustic levitator, quasicrystal placement, trap stability | quasicrystal placement improves trap stability | **RETRACTED** | single-seed "win" was the generator's arbitrary default; 15-seed check: only 3/15 (20%) matched it, 60% worse than baseline | `quasicrystal_array_check.py` |
| 10 | Spikeling real Resonator neuron, SSH-coupled bridge | topological defect-tolerance transfers to real neuromorphic hardware substrate | **INCONCLUSIVE/fragile** | win-rate 60%, 95% CI 35-85% (n=15) | `spikeling-phononic-bridge`, `ssh_resonator_bridge.py` |
| 11 | Spikeling real LIF chain, SSH-coupled | edge retains more memory capacity than bulk under hopping disorder | **CONFIRMED, robust** | 5/5 seeds, mean edge 70% vs bulk 22% | `spikeling-phononic-bridge`, `ssh_chiral_experiment.gd` |
| 12 | Spikeling real LIF chain, SSH-coupled | hopping disorder hurts edge mode more than on-site disorder (chiral-symmetry prediction) | **FAILED, wrong direction** | 0/5 seeds; on-site disorder preserves edge MC *better* (79% vs 70%) | same |
| 13 | Fibonacci-substitution connectivity vs random, density-matched | cheap-to-generate structure computes competitively | **CONFOUNDED** (initial) | gap widened 1.83x→3.32x with N — traced to a degree mismatch, not a real effect | `topological-phononics-finding`, `fibonacci_connectivity.py` |
| 14 | Fibonacci connectivity vs random, degree-matched + prefix-nested (corrected) | — | **real, but a modest cost, not a benefit** | 1.4-2x worse (linear recall), 1-17% worse (NARMA10) — cheap to *generate*, not better at *computing* | `fibonacci_prefix_sweep.py` et al. |
| 15 | Real 2D de Bruijn quasicrystal vs 1D Fibonacci vs random | genuine 2D quasi-periodicity closes the gap to random | **partial** | quasicrystal beats Fibonacci by ~30%, but both still lose to random outright | `debruijn_quasicrystal_reservoir.py` |
| 16 | RKKY magnetic exchange coupling vs dense random | a real solid-state coupling law makes a viable reservoir | **NEGATIVE** | 1.4-1.9x worse than random; capability itself borderline vacuous at 4/5 tested k_F | `rkky_magnetic_reservoir.py` |
| 17 | Ferromagnetic vs antiferromagnetic coupling | uniform-sign coupling variants perform comparably | **asymmetric NEGATIVE** | antiferro 4x worse than random on NARMA10 specifically; mechanism directly confirmed (negative dominant eigenvalue → temporal sign-alternation breaks nonlinear but not linear tasks) | same session, `rkky_magnetic_reservoir.py` follow-up |
| 18 | Structured vs random connectivity, real recorded audio drive | the "random beats structured" pattern is a synthetic-noise artifact | **NULL — pattern replicates on real data** | random beats every structured architecture on 2 independent real audio clips, ~1.5-1.7x | `real_audio_reservoir_test.py` |
| 19 | Acoustic MEMS quasicrystal-perforated plate | aperiodic perforation makes a better general reservoir | **NULL**, one narrow exception | computationally inert broadly; the one real effect is a bulletproof even-order symmetry-selection rule, real only on shallow tasks (depth ≤3) in the weakly-coupled regime | `reservoir-quasicrystal-finding` |
| 20 | Cross-substrate unification: one "symmetry-conditional protection" law | phononic/ternary/acoustic share one governing rule | **RETRACTED on measurement** | phononic and ternary rhyme narrowly (both: invariant protected, computation doesn't follow) but acoustic is a different mechanism and doesn't reproduce even within its own substrate class | `cross-substrate-synthesis-finding` |
| 21 | Jet-engine spike pipeline (real-world-motivated staged network, no lattice) | turbine→compressor feedback loop gives its neurons a genuine robustness advantage | **REVERSED after confound control** | naive read: reinforced region 0.52 vs feedforward-only 0.09 (looks strongly positive) — but confounded with hop-distance; controlling for depth (feedback removed entirely, same 1-hop distance) shows 0.66, i.e. feedback *lowers* robustness relative to the depth-matched control | `jet-engine-feedback-robustness-finding`, 2026-08-09, tonight |

## Tally

Of 21 rows: **3 confirmed** (#1, #2, #11 — all narrow and conditional even where positive: chiral-symmetry-gated, within-architecture only, degrading under nonlinearity/noise), **2 inconclusive** (#6, #10 — both lean positive but fail their own pre-registered bar), **16 null, refuted, reversed, or retracted**. Two of the confirmed cases (#1, #11) and both inconclusive cases (#6, #10) concern the *same* underlying object — SSH chiral-symmetric defect protection — tested on different substrates (idealized lattice, Duffing mechanical model, real Spikeling LIF chain, real Spikeling Resonator). So out of roughly a dozen *distinct* hypotheses tested (collapsing repeated tests of the same SSH claim into one), effectively **one** survives cleanly: chiral-symmetry-conditional topological defect-tolerance, and even that one does not transfer at full idealized magnitude to a real device (#5, #6) or to a differently-structured real task (#7).

## Discussion

**Why report this.** A single confirmed result and nineteen non-confirmations is not, on its face, an exciting headline — but the *pattern of failure* is informative in a way the individual negatives are not. The hypothesis "exotic/reinforcing structure improves computational robustness" was not tested once and abandoned; it was tested against topological lattices, magnetic coupling, quasi-periodic tilings, and a domain-specific engineered network, using genuinely different confound classes each time (input-locality, degree-matching, chiral symmetry, hop-distance) — and it lost or came back conditional nearly every time regardless of which specific confound was the threat. That is a real, if modest, calibration fact for anyone (including this researcher, going forward) proposing the next instance of this hypothesis shape: the prior should start skeptical, not favorable, and the burden of proof is a matched, confound-controlled baseline, not a raw comparison against an unmatched naive network.

**The one real exception is narrower than it first sounds.** Chiral-symmetry-conditional SSH defect-tolerance is the sole hypothesis in this ledger confirmed on more than one substrate (#1, #11) — but every attempt to broaden its scope failed: cross-architecture (#3, #4), full device physics without deliberate symmetry engineering (#5), a differently-structured real task (#7), and even the narrower "which disorder type" sub-prediction on the exact same LIF-chain substrate that confirmed the headline claim (#12). The lesson generalizes past this one mechanism: a structural effect confirmed under one specific set of conditions (task, substrate, disorder type, architecture comparison) should be assumed *not* to generalize to a change in any of those conditions until separately tested — confirmed in this ledger at least four separate times (#3 vs #1, #5 vs #1, #7 vs #1/#11, #12 vs #11).

**A recurring, specific failure mode: path/chain topologies are fragile by construction, not by disorder.** Rows #3, #4, and #21 independently converge on the same mechanism from three unrelated substrates: a linear-chain or feedback-dependent topology loses robustness not because structure fails to protect against *external* perturbation, but because the chain's own connectivity creates cut-vertices or single-point dependencies that a denser or simpler alternative doesn't have. In #21 specifically, the failure mode is new to this ledger: it is not that feedback fails to protect against unrelated disorder (the naive hypothesis), but that *depending on* feedback creates a longer, indirect dependency chain (the whole downstream cascade must fire once to arm the reinforcement) that a non-reinforced neuron with only two local synapses does not have. Denser/more-interdependent connectivity raising fragility, independently confirmed via three unrelated mechanisms (SSH path cut-vertices, RKKY/antiferro dense all-to-all coupling, and now jet-engine indirect feedback dependency), is arguably the most load-bearing sub-finding in this entire ledger.

**What would change this prior.** A hypothesis of this shape surviving (a) a matched/degree-controlled baseline, (b) more than one task type, (c) more than one disorder/perturbation class, and (d) a real (not idealized) physical or device-level substrate, simultaneously, would be a genuine break from the pattern above and worth treating as a real design primitive rather than a conditional curiosity. Nothing in this ledger has cleared all four simultaneously; #1/#2 clear (b) and part of (c); nothing clears (d) at full magnitude.

## Limitations

This is a synthesis of simulation studies (with one real-audio exception, #18, and one real-machine-telemetry exception, #21's driving data, though #21's network itself is simulated). The twenty-one rows are not a random or exhaustive sample of possible structure-vs-robustness hypotheses — they are the hypotheses this specific research program happened to test, largely because they were physically or mechanistically motivated at the time. A file-drawer effect in the *positive* direction is unlikely (every test here, positive or negative, is already on the public record), but the ledger cannot speak to hypotheses in this shape that were never attempted.

## Data and code availability

Rows #1-#9, #13-#15, #20 are documented in `topological-phononics/` (public, github.com/gbranaa4-hue/topological-phononics) and `acoustic-vortex-sim/reservoir_computing/FINDINGS.txt`. Rows #10-#12 are documented in the Spikeling project (github.com/gbranaa4-hue/Spikeling) vault entry `spikeling-phononic-bridge`. Rows #16-#19 are in `topological-phononics/` session scripts (`rkky_magnetic_reservoir.py`, `real_audio_reservoir_test.py`). Row #21 is `horde-beta-version-1/../Spikeling/ai-apps/jet_engine_feedback_robustness.py`, logged 2026-08-09 in the Spikeling vault as `jet-engine-feedback-robustness-finding`.

*Conducted under the gbranaa4-hue method: pre-register the test, validate the instrument, split the average, find the boundary, and write the "no" as carefully as the "yes."*
